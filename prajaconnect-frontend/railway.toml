[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "sh -c 'caddy run --config Caddyfile --adapter caddyfile'"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
